node_modules
dist
dist-ssr
.vite
.env
.env.local
*.log
.DS_Store

# Playwright e2e artifacts + ad-hoc UI-smoke scratch. Leading slash on
# the PNG match limits it to project root so public/*.png assets aren't
# caught. The committed e2e suite lives in /e2e (config: playwright.config.ts).
/*.png
/ui_smoke*.js
/test-results
/playwright-report
/blob-report
/playwright/.cache

# Local AI assistant configuration
.claude/

# Deployment / Environment config
vercel.json
.nvmrc
