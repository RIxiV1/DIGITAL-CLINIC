node_modules
dist
dist-ssr
.vite
.env
.env.local
*.log
.DS_Store

# Scratch / smoke artifacts (one-off scripts + their screenshot outputs).
# Leading slash limits the PNG match to project root so public/*.png
# assets aren't caught.
/*.png
/ui_smoke*.js
/tmp_*
/test-results

# Local AI assistant configuration
.claude/
