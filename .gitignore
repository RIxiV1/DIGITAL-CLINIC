node_modules
dist
dist-ssr
.vite
.env
.env.local
*.log
.DS_Store

# UI-smoke artifacts (Playwright isn't an npm dep — these are
# scratch scripts + their screenshot outputs). Leading slash limits
# the PNG match to project root so public/*.png assets aren't caught.
/*.png
/ui_smoke*.js
/test-results

# Local AI assistant configuration
.claude/

# Deployment / Environment config
vercel.json
.nvmrc
